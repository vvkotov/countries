import { InputControlDirective } from './input-control.directive';

describe('InputControlDirective', () => {
  it('should create an instance', () => {
    const directive = new InputControlDirective();
    expect(directive).toBeTruthy();
  });
});
